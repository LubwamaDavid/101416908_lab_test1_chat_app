The app contains a signup page that will ask for user credentials such as username, name and password then you will use these credentials to login and access the chat app. In the chat app there is a drop down with 2 room options to choose from and once the user chooses the room, they can access the chat and send messages user is able to switch between rooms and logout of application once finished.